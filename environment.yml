name: differential_sim
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pytorch=2.5.1
  - pytorch-cuda=12.4
  - numpy
  - scipy
  - pandas
  - matplotlib
  - pyyaml
  - pytest
  - pip:
      - diffidm==0.0.3
variables:
  MPLCONFIGDIR: /tmp/matplotlib
